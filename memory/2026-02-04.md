# 2026-02-04 Memory

## Doge-Wallet Low Balance Notification Backoff

### Problem Solved
Low balance alerts were spamming every 30 minutes. User wanted reduced frequency.

### Solution Implemented
Added configurable `lowBalanceAlertIntervalHours` to doge-wallet plugin:
- Default: 24 hours between notifications
- Persists `lastNotifiedAt` timestamp to `~/.openclaw/doge/alert-state.json`
- Inline buttons (Dismiss/Snooze) still work for manual overrides

### Files Modified
- `~/.openclaw/extensions/doge-wallet/src/types.ts` - Added config option
- `~/.openclaw/extensions/doge-wallet/src/config.ts` - Added default (24h)
- `~/.openclaw/extensions/doge-wallet/src/alert-state.ts` - Added `shouldAlertWithInterval()`, `getTimeUntilNextAlert()`, updated `recordAlert()` to save timestamp
- `~/.openclaw/extensions/doge-wallet/index.ts` - Uses new interval check

### Current Status
- Plugin compiled and gateway restarted
- Balance: 9.69 DOGE (below 100 DOGE threshold)
- Waiting to verify test notification with inline buttons works
- Gateway schema doesn't recognize new field yet (plugin uses internal default)

### Key Paths
- Plugin: `~/.openclaw/extensions/doge-wallet/`
- Alert state: `~/.openclaw/doge/alert-state.json`
- Telegram chat ID: `8511108690`
