{
  "name": "@quackstro/openclaw-brain",
  "version": "2026.3.5",
  "private": true,
  "type": "module",
  "description": "Brain — behavioral memory system for OpenClaw. Auto-classifies thoughts into semantic buckets with action detection.",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@lancedb/lancedb": "^0.26.2",
    "@sinclair/typebox": "0.34.48"
  },
  "devDependencies": {
    "vitest": "^4.0.0",
    "typescript": "^5.7.3"
  },
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
